# 飞书知识库文档管理规则

## 本地开发与 Worktree 规则（全局唯一）

- `/Users/yuezhenting/esp/xiaozhi-175c-dev` 默认只允许在本地 `main` 分支开发、测试、构建和打包。
- 未收到用户明确开启 Worktree 模式的指令时，任何 AI 工具都不得创建、使用或从 Worktree 构建发布；需要隔离时必须先停止并报告，不能擅自切换。
- 已存在的历史 Worktree 仅用于审计和迁移，不得作为新的开发或发布事实源；确认没有未合并的有效提交后应移除并执行 `git worktree prune`。
- `main` 是唯一代码事实源；分支代码合并前必须逐提交核对，禁止用整条分支覆盖本地未提交或已验证的代码。

## 管理范围

- 飞书知识库根节点：<https://kwop6mh235.feishu.cn/wiki/U08RwzBAiiHW4okNtvlcur2Nn6g>
- 只允许读取、搜索、遍历、创建和编辑该根节点及其所有子节点。
- 不得操作上述根节点之外的飞书知识库。

## 操作规范

- 飞书操作优先使用官方 `lark-cli`。
- 按场景使用 `lark-wiki`、`lark-doc`、`lark-drive` Skills。
- 默认使用 `--as user`。
- 用户已允许自动读取、搜索、遍历根节点及全部子节点。
- 编辑文档前必须先 fetch 最新版本。
- 编辑完成后必须重新 fetch，验证修改结果。
- 移动或删除文档/节点前，必须先列出将受影响的对象。
- 删除知识空间、批量删除、权限变更等高风险操作，必须先获得用户明确授权。
